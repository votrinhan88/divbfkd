name: divbfkd
channels:
  - defaults
dependencies:
  - pip
  - python=3.10
  - pip:
      - ipykernel
      - matplotlib
      - numpy
      - pandas
      - pyyaml
      - torch
      - torchvision
      - torchinfo
      - tqdm
      - umap-learn